"use server"

import { InquiryValues } from "./HomeInquiry"

export default async function SubmitHomeInquiry (data: InquiryValues) {

    // Call a post request here
    // Data will be sent to GoHighLevel CRM
    // Not a lead, only inquiry

    return (
        <div>
        </div>
    )
}