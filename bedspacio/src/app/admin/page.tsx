
import Add from '@/asset/icon/add.svg';

export default function Dashboard () {

    return (
        <div className="flex w-full min-h-screen items-center justify-center">
            DASHBOARD PAGE (will show statistics later on, work in progress)
        </div>
    )
}